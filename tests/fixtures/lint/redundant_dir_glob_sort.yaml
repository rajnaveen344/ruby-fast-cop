cop: Lint/RedundantDirGlobSort
department: lint
severity: warning
implemented: false

tests:
  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_registers_an_offense_and_correction_when_using_dir_glob_sort
    source: |
      Dir.glob(Rails.root.join('test', '*.rb')).sort.each(&method(:require))
    offenses:
      - line: 1
        column_start: 42
        column_end: 46
        message: Remove redundant `sort`.
    corrected: |
      Dir.glob(Rails.root.join('test', '*.rb')).each(&method(:require))
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_registers_an_offense_and_correction_when_using_dir_glob_sort
    source: |
      ::Dir.glob(Rails.root.join('test', '*.rb')).sort.each(&method(:require))
    offenses:
      - line: 1
        column_start: 44
        column_end: 48
        message: Remove redundant `sort`.
    corrected: |
      ::Dir.glob(Rails.root.join('test', '*.rb')).each(&method(:require))
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_registers_an_offense_and_correction_when_using_dir_sort_each_do
    source: |
      Dir['./lib/**/*.rb'].sort.each do |file|
      end
    offenses:
      - line: 1
        column_start: 21
        column_end: 25
        message: Remove redundant `sort`.
    corrected: |
      Dir['./lib/**/*.rb'].each do |file|
      end
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_registers_an_offense_and_correction_when_using_dir_sort_each_do_something
    source: |
      Dir['./lib/**/*.rb'].sort.each(&method(:require))
    offenses:
      - line: 1
        column_start: 21
        column_end: 25
        message: Remove redundant `sort`.
    corrected: |
      Dir['./lib/**/*.rb'].each(&method(:require))
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_not_using_sort_with_sort_false_option_for_dir
    source: |
      Dir.glob(Rails.root.join('test', '*.rb'), sort: false).each do
      end
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_using_dir_glob_b_txt_a_txt_sort
    source: |
      Dir.glob('./b/*.txt', './a/*.txt').sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_using_dir_glob_path_sort
    source: |
      Dir.glob(*path).sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_using_dir_b_txt_a_txt_sort
    source: |
      Dir['./b/*.txt', './a/*.txt'].sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_using_dir_path_sort
    source: |
      Dir[*path].sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_3_0_or_higher_does_not_register_an_offense_when_using_collection_sort
    source: |
      collection.sort
    offenses:
      []
    ruby_version: ">= 3.0"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_2_7_or_lower_does_not_register_an_offense_when_using_dir_glob_sort
    source: |
      Dir.glob(Rails.root.join('test', '*.rb')).sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 2.7"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_2_7_or_lower_does_not_register_an_offense_when_using_dir_glob_sort
    source: |
      ::Dir.glob(Rails.root.join('test', '*.rb')).sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 2.7"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_2_7_or_lower_does_not_register_an_offense_when_using_dir_sort_each_do
    source: |
      Dir['./lib/**/*.rb'].sort.each do |file|
      end
    offenses:
      []
    ruby_version: ">= 2.7"

  - name: rubocop_cop_lint_redundantdirglobsort_when_ruby_2_7_or_lower_does_not_register_an_offense_when_using_dir_sort_each_do_something
    source: |
      Dir['./lib/**/*.rb'].sort.each(&method(:require))
    offenses:
      []
    ruby_version: ">= 2.7"

  - name: rubocop_cop_lint_redundantdirglobsort_does_not_register_an_offense_when_not_using_sort_for_dir
    source: |
      Dir['./lib/**/*.rb'].each do |file|
      end
    offenses:
      []

  - name: rubocop_cop_lint_redundantdirglobsort_does_not_register_an_offense_when_using_sort_without_a_receiver
    source: |
      sort.do_something
    offenses:
      []
