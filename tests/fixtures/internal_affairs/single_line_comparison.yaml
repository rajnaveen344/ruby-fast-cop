cop: InternalAffairs/SingleLineComparison
department: internal_affairs
severity: convention
implemented: false

tests:
  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_loc_first_line_with_loc_last_line
    source: |
      node.loc.first_line == node.loc.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 41
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_loc_last_line_with_loc_first_line
    source: |
      node.loc.last_line == node.loc.first_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 41
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_loc_line_with_loc_last_line
    source: |
      node.loc.line == node.loc.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 35
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_loc_last_line_with_loc_line
    source: |
      node.loc.last_line == node.loc.line
    offenses:
      - line: 1
        column_start: 0
        column_end: 35
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_source_range_first_line_with_source_range_last_line
    source: |
      node.source_range.first_line == node.source_range.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 59
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_source_range_last_line_with_source_range_first_line
    source: |
      node.source_range.last_line == node.source_range.first_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 59
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_first_line_with_last_line
    source: |
      node.first_line == node.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 33
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_comparing_last_line_with_first_line
    source: |
      node.last_line == node.first_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 33
        message: Use `node.single_line?`.
    corrected: |
      node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_negative_comparing_first_line_with_last_line
    source: |
      node.first_line != node.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 33
        message: Use `!node.single_line?`.
    corrected: |
      ‹BASE›0‹/BASE›
      !node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_registers_and_corrects_an_offense_when_negative_comparing_last_line_with_first_line
    source: |
      node.last_line != node.first_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 33
        message: Use `!node.single_line?`.
    corrected: |
      ‹BASE›0‹/BASE›
      !node.single_line?

  - name: rubocop_cop_internalaffairs_singlelinecomparison_does_not_register_an_offense_when_comparing_the_same_line
    source: |
      node.loc.first_line == node.loc.line
    offenses:
      []

  - name: rubocop_cop_internalaffairs_singlelinecomparison_does_not_register_an_offense_when_the_receivers_are_not_a_match
    source: |
      nodes.first.first_line == nodes.last.last_line
    offenses:
      []

  - name: rubocop_cop_internalaffairs_singlelinecomparison_with_safe_navigation_registers_an_offense_and_corrects
    source: |
      node&.first_line == node&.last_line
      node&.first_line != node&.last_line
    offenses:
      - line: 1
        column_start: 0
        column_end: 35
        message: Use `node&.single_line?`.
      - line: 2
        column_start: 0
        column_end: 35
        message: Use `!node&.single_line?`.
    corrected: |
      ‹BASE›0‹/BASE›
      node&.single_line?
      !node&.single_line?
