cop: InternalAffairs/NodeTypePredicate
department: internal_affairs
severity: convention
implemented: false

tests:
  - name: rubocop_cop_internalaffairs_nodetypepredicate_comparison_node_type_check_registers_an_offense_and_autocorrects
    source: |
      node.type == :send
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: "Use `#send_type?` to check node type."
    corrected: |
      node.send_type?

  - name: rubocop_cop_internalaffairs_nodetypepredicate_comparison_node_type_check_with_safe_navigation_operator_registers_an_offense_and_autocorrects
    source: |
      node&.type == :send
    offenses:
      - line: 1
        column_start: 0
        column_end: 19
        message: "Use `#send_type?` to check node type."
    corrected: |
      node&.send_type?

  - name: rubocop_cop_internalaffairs_nodetypepredicate_does_not_register_an_offense_for_a_predicate_node_type_check
    source: |
      node.send_type?
    offenses:
      []
