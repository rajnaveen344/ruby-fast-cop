cop = "InternalAffairs/RedundantLetRubocopConfigNew"
department = "internal_affairs"
severity = "convention"
implemented = false

[[tests]]
name = "rubocop_cop_internalaffairs_redundantletrubocopconfignew_registers_an_offense_when_using_let_config_and_config_is_not_specified_in_describe"
source = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma do
  subject(:cop) { described_class.new(config) }

  let(:config) { RuboCop::Config.new }
end
'''
corrected = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new(config) }

end
'''

[[tests.offenses]]
line = 4
column_start = 2
column_end = 38
message = "Remove `let` that is `RuboCop::Config.new` with no arguments and specify `:config` in `describe`."

[[tests]]
name = "rubocop_cop_internalaffairs_redundantletrubocopconfignew_registers_an_offense_when_using_let_config_and_config_is_already_specified_in_describe"
source = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new(config) }

  let(:config) { RuboCop::Config.new }
end
'''
corrected = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new(config) }

end
'''

[[tests.offenses]]
line = 4
column_start = 2
column_end = 38
message = "Remove `let` that is `RuboCop::Config.new` with no arguments."

[[tests]]
name = "rubocop_cop_internalaffairs_redundantletrubocopconfignew_registers_an_offense_when_using_let_config_with_no_argument_rubocop_config_new_and_config_is_specified"
source = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new }

  let(:config) { RuboCop::Config.new }
end
'''
corrected = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new }

end
'''

[[tests.offenses]]
line = 4
column_start = 2
column_end = 38
message = "Remove `let` that is `RuboCop::Config.new` with no arguments."

[[tests]]
name = "rubocop_cop_internalaffairs_redundantletrubocopconfignew"
source = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new(config) }

  let(:config) { RuboCop::Config.new(described_class.badge.to_s => cop_config) }

  let(:cop_config) { { 'Parameter' => 'Value' } }
end
'''
corrected = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma, :config do
  subject(:cop) { described_class.new(config) }


  let(:cop_config) { { 'Parameter' => 'Value' } }
end
'''

[[tests.offenses]]
line = 4
column_start = 2
column_end = 80
message = "Remove `let` that is `RuboCop::Config.new` with no arguments."

[[tests]]
name = "rubocop_cop_internalaffairs_redundantletrubocopconfignew_does_not_register_an_offense_when_using_let_config_with_arguments_to_rubocop_config_new"
source = '''
RSpec.describe RuboCop::Cop::Layout::SpaceAfterComma do
  let(:config) { RuboCop::Config.new('Layout/SpaceInsideHashLiteralBraces' => brace_config) }
end
'''
offenses = []
