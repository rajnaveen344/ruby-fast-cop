cop: Style/ReturnNil
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_returnnil_when_enforced_style_is_return_registers_an_offense_for_return_nil
    source: |
      return nil
    offenses:
      - line: 1
        column_start: 0
        column_end: 10
        message: Use `return` instead of `return nil`.
    corrected: |
      return

  - name: rubocop_cop_style_returnnil_when_enforced_style_is_return_does_not_register_an_offense_for_returning_others
    source: |
      return 2
    offenses:
      []

  - name: rubocop_cop_style_returnnil_when_enforced_style_is_return_does_not_register_an_offense_for_return_nil_from_iterators
    source: |
      loop do
        return if x
      end
    offenses:
      []

  - name: rubocop_cop_style_returnnil_when_enforced_style_is_return_nil_registers_an_offense_for_return
    source: |
      return
    offenses:
      - line: 1
        column_start: 0
        column_end: 6
        message: Use `return nil` instead of `return`.
    corrected: |
      return nil

  - name: rubocop_cop_style_returnnil_when_enforced_style_is_return_nil_does_not_register_an_offense_for_returning_others
    source: |
      return 2
    offenses:
      []
