cop: Style/ReverseFind
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_find_with_block
    source: |
      array.reverse.find { |item| item.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 18
        message: Use `rfind` instead.
    corrected: |
      array.rfind { |item| item.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_find_with_block
    source: |
      array&.reverse&.find { |item| item.even? }
    offenses:
      - line: 1
        column_start: 7
        column_end: 20
        message: Use `rfind` instead.
    corrected: |
      array&.rfind { |item| item.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_find_with_numbered_block
    source: |
      array.reverse.find { _1.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 18
        message: Use `rfind` instead.
    corrected: |
      array.rfind { _1.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_find_with_it_block
    source: |
      array.reverse.find { it.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 18
        message: Use `rfind` instead.
    corrected: |
      array.rfind { it.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_detect_with_block
    source: |
      array.reverse.detect { |item| item.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 20
        message: Use `rfind` instead.
    corrected: |
      array.rfind { |item| item.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_each_find_with_block
    source: |
      array.reverse_each.find { |item| item.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 23
        message: Use `rfind` instead.
    corrected: |
      array.rfind { |item| item.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_each_detect_with_block
    source: |
      array.reverse_each.detect { |item| item.even? }
    offenses:
      - line: 1
        column_start: 6
        column_end: 25
        message: Use `rfind` instead.
    corrected: |
      array.rfind { |item| item.even? }
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_find_with_symbol_proc
    source: |
      array.reverse.find(&:even?)
    offenses:
      - line: 1
        column_start: 6
        column_end: 18
        message: Use `rfind` instead.
    corrected: |
      array.rfind(&:even?)
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_registers_an_offense_when_using_reverse_detect_with_symbol_proc
    source: |
      array.reverse.detect(&:even?)
    offenses:
      - line: 1
        column_start: 6
        column_end: 20
        message: Use `rfind` instead.
    corrected: |
      array.rfind(&:even?)
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_4_0_does_not_register_an_offense_when_using_rfind
    source: |
      array.rfind { |item| item.even? }
    offenses:
      []
    ruby_version: ">= 4.0"

  - name: rubocop_cop_style_reversefind_when_ruby_3_4_does_not_register_an_offense_when_using_reverse_find_with_block
    source: |
      array.reverse.find { |item| item.even? }
    offenses:
      []
    ruby_version: ">= 3.4"

  - name: rubocop_cop_style_reversefind_when_ruby_3_4_does_not_register_an_offense_when_using_reverse_detect_with_block
    source: |
      array.reverse.detect { |item| item.even? }
    offenses:
      []
    ruby_version: ">= 3.4"

  - name: rubocop_cop_style_reversefind_when_ruby_3_4_does_not_register_an_offense_when_using_reverse_find_with_symbol_proc
    source: |
      array.reverse.find(&:even?)
    offenses:
      []
    ruby_version: ">= 3.4"

  - name: rubocop_cop_style_reversefind_when_ruby_3_4_does_not_register_an_offense_when_using_reverse_detect_with_symbol_proc
    source: |
      array.reverse.detect(&:even?)
    offenses:
      []
    ruby_version: ">= 3.4"
