cop: Style/MissingElse
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_missingelse_ruby_2_7_does_not_register_an_offense
    source: |
      case pattern; in a; foo; end
    offenses:
      []
    ruby_version: ">= 2.7"

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_if_statement_with_no_else_clause_registers_an_offense
    source: |
      if cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 16
        message: "`if` condition requires an `else`-clause."

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_an_unless_statement_with_no_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_enabled_given_a_case_statement_with_no_else_clause_registers_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      - line: 1
        column_start: 0
        column_end: 37
        message: "`case` condition requires an `else`-clause."

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_if_statement_with_no_else_clause_registers_an_offense
    source: |
      if cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 16
        message: "`if` condition requires an `else`-clause."

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_an_unless_statement_with_no_else_clause_registers_an_offense
    source: |
      unless cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: "`if` condition requires an `else`-clause."

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_unlesselse_disabled_given_a_case_statement_with_no_else_clause_registers_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      - line: 1
        column_start: 0
        column_end: 37
        message: "`case` condition requires an `else`-clause."

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_no_else_clause_registers_an_offense
    source: |
      if cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 16
        message: "`if` condition requires an `else`-clause with `nil` in it."
    corrected: |
      if cond; foo else; nil; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_no_else_clause_registers_an_offense_with_elsif_clause
    source: |
      if cond_1; 1; elsif cond_2; 3; end
    offenses:
      - line: 1
        column_start: 14
        column_end: 29
        message: "`if` condition requires an `else`-clause with `nil` in it."
    corrected: |
      if cond_1; 1; elsif cond_2; 3; else; nil; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_if_statement_with_no_else_clause_registers_an_offense_with_multiple_elsif_clauses
    source: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; end
    offenses:
      - line: 1
        column_start: 31
        column_end: 46
        message: "`if` condition requires an `else`-clause with `nil` in it."
    corrected: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; else; nil; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_an_unless_statement_with_no_else_clause_registers_an_offense
    source: |
      unless cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: "`if` condition requires an `else`-clause with `nil` in it."
    corrected: |
      unless cond; foo else; nil; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_empty_given_a_case_statement_with_no_else_clause_registers_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      - line: 1
        column_start: 0
        column_end: 37
        message: "`case` condition requires an `else`-clause with `nil` in it."
    corrected: |
      case v; when a; foo; when b; bar; else; nil; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_no_else_clause_registers_an_offense
    source: |
      if cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 16
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond; foo else; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_no_else_clause_registers_an_offense_with_elsif_clause
    source: |
      if cond_1; 1; elsif cond_2; 3; end
    offenses:
      - line: 1
        column_start: 14
        column_end: 29
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond_1; 1; elsif cond_2; 3; else; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_if_statement_with_no_else_clause_registers_an_offense_with_multiple_elsif_clauses
    source: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; end
    offenses:
      - line: 1
        column_start: 31
        column_end: 46
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; else; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_an_unless_statement_with_no_else_clause_registers_an_offense
    source: |
      unless cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      unless cond; foo else; end

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_emptyelse_enabled_and_set_to_warn_on_nil_given_a_case_statement_with_no_else_clause_registers_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      - line: 1
        column_start: 0
        column_end: 37
        message: "`case` condition requires an empty `else`-clause."
    corrected: |
      case v; when a; foo; when b; bar; else; end

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_no_else_clause_registers_an_offense
    source: |
      if cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 16
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond; foo else; end

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_no_else_clause_registers_an_offense_with_elsif_clause
    source: |
      if cond_1; 1; elsif cond_2; 3; end
    offenses:
      - line: 1
        column_start: 14
        column_end: 29
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond_1; 1; elsif cond_2; 3; else; end

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_if_statement_with_no_else_clause_registers_an_offense_with_multiple_elsif_clauses
    source: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; end
    offenses:
      - line: 1
        column_start: 31
        column_end: 46
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      if cond_1; 1; elsif cond_2; 2; elsif cond_3; 3; else; end

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_an_unless_statement_with_no_else_clause_registers_an_offense
    source: |
      unless cond; foo end
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: "`if` condition requires an empty `else`-clause."
    corrected: |
      unless cond; foo else; end

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_if_given_a_case_statement_with_no_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_if_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      if a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_if_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      if a; foo elsif b; bar else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_if_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      if cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_if_statement_with_no_else_clause_doesn_t_register_an_offense
    source: |
      if cond; foo end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_unless_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_unless_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      unless cond; foo else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_unless_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      unless cond; foo else bar; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_an_unless_statement_with_no_else_clause_doesn_t_register_an_offense
    source: |
      unless cond; foo end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_a_case_statement_with_a_completely_empty_else_clause_doesn_t_register_an_offense
    source: |
      case v; when a; foo else end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_a_case_statement_with_an_else_clause_containing_only_the_literal_nil_doesn_t_register_an_offense
    source: |
      case v; when a; foo; when b; bar; else nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_a_case_statement_with_an_else_clause_with_side_effects_doesn_t_register_an_offense
    source: |
      case v; when a; foo; else b; nil end
    offenses:
      []

  - name: rubocop_cop_style_missingelse_configured_to_warn_only_on_empty_case_given_a_case_statement_with_no_else_clause_registers_an_offense
    source: |
      case v; when a; foo; when b; bar; end
    offenses:
      - line: 1
        column_start: 0
        column_end: 37
        message: "`case` condition requires an empty `else`-clause."
    corrected: |
      case v; when a; foo; when b; bar; else; end
