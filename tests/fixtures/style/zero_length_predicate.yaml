cop: Style/ZeroLengthPredicate
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_0
    source: |
      [1, 2, 3].length == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 21
        message: Use `empty?` instead of `length == 0`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_0
    source: |
      [1, 2, 3]&.length == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 22
        message: Use `empty?` instead of `length == 0`.
    corrected: |
      [1, 2, 3]&.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_0
    source: |
      [1, 2, 3].size == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 19
        message: Use `empty?` instead of `size == 0`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_zero
    source: |
      [1, 2, 3].length.zero?
    offenses:
      - line: 1
        column_start: 10
        column_end: 22
        message: Use `empty?` instead of `length.zero?`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_zero
    source: |
      [1, 2, 3]&.length.zero?
    offenses:
      - line: 1
        column_start: 11
        column_end: 23
        message: Use `empty?` instead of `length.zero?`.
    corrected: |
      [1, 2, 3]&.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_zero
    source: |
      [1, 2, 3]&.length&.zero?
    offenses:
      - line: 1
        column_start: 11
        column_end: 24
        message: Use `empty?` instead of `length&.zero?`.
    corrected: |
      [1, 2, 3]&.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_zero
    source: |
      [1, 2, 3].size.zero?
    offenses:
      - line: 1
        column_start: 10
        column_end: 20
        message: Use `empty?` instead of `size.zero?`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_length
    source: |
      0 == [1, 2, 3].length
    offenses:
      - line: 1
        column_start: 0
        column_end: 21
        message: Use `empty?` instead of `0 == length`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_size
    source: |
      0 == [1, 2, 3].size
    offenses:
      - line: 1
        column_start: 0
        column_end: 19
        message: Use `empty?` instead of `0 == size`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_1
    source: |
      [1, 2, 3].length < 1
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `empty?` instead of `length < 1`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_1
    source: |
      array&.length < 1
    offenses:
      - line: 1
        column_start: 0
        column_end: 17
        message: Use `empty?` instead of `length < 1`.
    corrected: |
      array&.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_1
    source: |
      [1, 2, 3].size < 1
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `empty?` instead of `size < 1`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_1_array_length
    source: |
      1 > [1, 2, 3].length
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `empty?` instead of `1 > length`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_1_array_length
    source: |
      1 > array&.length
    offenses:
      - line: 1
        column_start: 0
        column_end: 17
        message: Use `empty?` instead of `1 > length`.
    corrected: |
      array&.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_1_array_size
    source: |
      1 > [1, 2, 3].size
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `empty?` instead of `1 > size`.
    corrected: |
      [1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_0
    source: |
      [1, 2, 3].length > 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `!empty?` instead of `length > 0`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_does_not_register_an_offense_for_array_length_0
    source: |
      [1, 2, 3]&.length > 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_0
    source: |
      [1, 2, 3].size > 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `!empty?` instead of `size > 0`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_0
    source: |
      [1, 2, 3].length != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 21
        message: Use `!empty?` instead of `length != 0`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_0
    source: |
      [1, 2, 3].size != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 19
        message: Use `!empty?` instead of `size != 0`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_length_zero
    source: |
      ![1, 2, 3].length.zero?
    offenses:
      - line: 1
        column_start: 11
        column_end: 23
        message: Use `empty?` instead of `length.zero?`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_array_size_zero
    source: |
      ![1, 2, 3].size.zero?
    offenses:
      - line: 1
        column_start: 11
        column_end: 21
        message: Use `empty?` instead of `size.zero?`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_length
    source: |
      0 < [1, 2, 3].length
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `!empty?` instead of `0 < length`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_size
    source: |
      0 < [1, 2, 3].size
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `!empty?` instead of `0 < size`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_length
    source: |
      0 != [1, 2, 3].length
    offenses:
      - line: 1
        column_start: 0
        column_end: 21
        message: Use `!empty?` instead of `0 != length`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_arrays_registers_an_offense_for_0_array_size
    source: |
      0 != [1, 2, 3].size
    offenses:
      - line: 1
        column_start: 0
        column_end: 19
        message: Use `!empty?` instead of `0 != size`.
    corrected: |
      ![1, 2, 3].empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_hashes_registers_an_offense_for_hash_size_0
    source: |
      { a: 1, b: 2 }.size == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 24
        message: Use `empty?` instead of `size == 0`.
    corrected: |
      { a: 1, b: 2 }.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_hashes_registers_an_offense_for_0_hash_size
    source: |
      0 == { a: 1, b: 2 }.size
    offenses:
      - line: 1
        column_start: 0
        column_end: 24
        message: Use `empty?` instead of `0 == size`.
    corrected: |
      { a: 1, b: 2 }.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_hashes_registers_an_offense_for_hash_size_0
    source: |
      { a: 1, b: 2 }.size != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 24
        message: Use `!empty?` instead of `size != 0`.
    corrected: |
      !{ a: 1, b: 2 }.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_hashes_registers_an_offense_for_0_hash_size
    source: |
      0 != { a: 1, b: 2 }.size
    offenses:
      - line: 1
        column_start: 0
        column_end: 24
        message: Use `!empty?` instead of `0 != size`.
    corrected: |
      !{ a: 1, b: 2 }.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_strings_registers_an_offense_for_string_size_0
    source: |
      "string".size == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `empty?` instead of `size == 0`.
    corrected: |
      "string".empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_strings_registers_an_offense_for_0_string_size
    source: |
      0 == "string".size
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `empty?` instead of `0 == size`.
    corrected: |
      "string".empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_strings_registers_an_offense_for_string_size_0
    source: |
      "string".size != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `!empty?` instead of `size != 0`.
    corrected: |
      !"string".empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_strings_registers_an_offense_for_0_string_size
    source: |
      0 != "string".size
    offenses:
      - line: 1
        column_start: 0
        column_end: 18
        message: Use `!empty?` instead of `0 != size`.
    corrected: |
      !"string".empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_collection_variables_registers_an_offense_for_collection_size_0
    source: |
      collection.size == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `empty?` instead of `size == 0`.
    corrected: |
      collection.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_collection_variables_registers_an_offense_for_0_collection_size
    source: |
      0 == collection.size
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `empty?` instead of `0 == size`.
    corrected: |
      collection.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_collection_variables_registers_an_offense_for_collection_size_0
    source: |
      collection.size != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `!empty?` instead of `size != 0`.
    corrected: |
      !collection.empty?

  - name: rubocop_cop_style_zerolengthpredicate_with_collection_variables_registers_an_offense_for_0_collection_size
    source: |
      0 != collection.size
    offenses:
      - line: 1
        column_start: 0
        column_end: 20
        message: Use `!empty?` instead of `0 != size`.
    corrected: |
      !collection.empty?

  - name: rubocop_cop_style_zerolengthpredicate_when_name_of_the_variable_is_size_or_length_accepts_equality_check
    source: |
      0 == length
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_name_of_the_variable_is_size_or_length_accepts_comparison
    source: |
      0 > length
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_name_of_the_variable_is_size_or_length_accepts_inequality_check
    source: |
      0 != length
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_stat_object_does_not_register_an_offense
    source: |
      File.stat(foo).size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_stat_object_does_not_register_an_offense_with_file
    source: |
      ::File.stat(foo).size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_with_a_string_does_not_register_an_offense_using_size_0
    source: |
      StringIO.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_with_a_string_does_not_register_an_offense_with_top_level_stringio_using_size_0
    source: |
      ::StringIO.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_with_a_string_does_not_register_an_offense_using_size_zero
    source: |
      StringIO.new('foo').size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_with_a_string_does_not_register_an_offense_with_top_level_stringio_using_size_zero
    source: |
      ::StringIO.new('foo').size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_without_arguments_does_not_register_an_offense_using_size_0
    source: |
      StringIO.new.size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_without_arguments_does_not_register_an_offense_with_top_level_stringio_using_size_0
    source: |
      ::StringIO.new.size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_without_arguments_does_not_register_an_offense_using_size_zero
    source: |
      StringIO.new.size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_stringio_object_when_initialized_without_arguments_does_not_register_an_offense_with_top_level_stringio_using_size_zero
    source: |
      ::StringIO.new.size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_object_does_not_register_an_offense_using_size_0
    source: |
      File.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_object_does_not_register_an_offense_with_top_level_file_using_size_0
    source: |
      ::File.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_object_does_not_register_an_offense_using_size_zero
    source: |
      File.new('foo').size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_file_object_does_not_register_an_offense_with_top_level_file_using_size_zero
    source: |
      ::File.new('foo').size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_tempfile_object_does_not_register_an_offense_using_size_0
    source: |
      Tempfile.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_tempfile_object_does_not_register_an_offense_with_top_level_tempfile_using_size_0
    source: |
      ::Tempfile.new('foo').size == 0
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_tempfile_object_does_not_register_an_offense_using_size_zero
    source: |
      Tempfile.new('foo').size.zero?
    offenses:
      []

  - name: rubocop_cop_style_zerolengthpredicate_when_inspecting_a_tempfile_object_does_not_register_an_offense_with_top_level_tempfile_using_size_zero
    source: |
      ::Tempfile.new('foo').size.zero?
    offenses:
      []
