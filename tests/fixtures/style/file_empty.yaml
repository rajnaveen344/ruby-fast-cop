cop: Style/FileEmpty
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_zero
    source: |
      File.zero?('path/to/file')
    offenses:
      - line: 1
        column_start: 0
        column_end: 26
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_zero
    source: |
      ‹BASE›0‹/BASE›
      !File.zero?('path/to/file')
    offenses:
      - line: 1
        column_start: 1
        column_end: 27
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      ‹BASE›0‹/BASE›
      !File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_zero_with_line_break
    source: |
      File.
        zero?('path/to/file')
    offenses:
      - line: 1
        column_start: 0
        column_end: 5
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_filetest_zero
    source: |
      FileTest.zero?('path/to/file')
    offenses:
      - line: 1
        column_start: 0
        column_end: 30
        message: "Use `FileTest.empty?('path/to/file')` instead."
    corrected: |
      FileTest.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_size_0
    source: |
      File.size('path/to/file') == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 30
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_size_0
    source: |
      ‹BASE›0‹/BASE›
      !File.size('path/to/file') == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 31
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      ‹BASE›0‹/BASE›
      !File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_size_0
    source: |
      File.size('path/to/file') >= 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 30
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      ‹BASE›0‹/BASE›
      !File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_size_0
    source: |
      ‹BASE›0‹/BASE›
      !File.size('path/to/file') >= 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 31
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_size_zero
    source: |
      File.size('path/to/file').zero?
    offenses:
      - line: 1
        column_start: 0
        column_end: 31
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_read_empty
    source: |
      File.read('path/to/file').empty?
    offenses:
      - line: 1
        column_start: 0
        column_end: 32
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_binread_empty
    source: |
      File.binread('path/to/file').empty?
    offenses:
      - line: 1
        column_start: 0
        column_end: 35
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_read
    source: |
      File.read('path/to/file') == ''
    offenses:
      - line: 1
        column_start: 0
        column_end: 31
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_binread
    source: |
      File.binread('path/to/file') == ''
    offenses:
      - line: 1
        column_start: 0
        column_end: 34
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_read
    source: |
      File.read('path/to/file') != ''
    offenses:
      - line: 1
        column_start: 0
        column_end: 31
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      ‹BASE›0‹/BASE›
      !File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_registers_an_offense_for_file_binread
    source: |
      File.binread('path/to/file') != ''
    offenses:
      - line: 1
        column_start: 0
        column_end: 34
        message: "Use `File.empty?('path/to/file')` instead."
    corrected: |
      ‹BASE›0‹/BASE›
      !File.empty?('path/to/file')
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_empty
    source: |
      File.empty?("path/to/file")
    offenses:
      []
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_non_offending_methods
    source: |
      File.exist?("path/to/file")
    offenses:
      []
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_zero
    source: |
      File.zero?('path/to/file')
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_filetest_zero
    source: |
      FileTest.zero?('path/to/file')
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_size_0
    source: |
      File.size('path/to/file') == 0
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_size_zero
    source: |
      File.size('path/to/file').zero?
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_read_empty
    source: |
      File.read('path/to/file').empty?
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_binread_empty
    source: |
      File.binread('path/to/file').empty?
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_read
    source: |
      File.read('path/to/file') == ''
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_binread
    source: |
      File.binread('path/to/file') == ''
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_file_empty
    source: |
      File.empty?("path/to/file")
    offenses:
      []
    ruby_version: ">= 2.3"

  - name: rubocop_cop_style_fileempty_target_ruby_version_2_4_does_not_register_an_offense_for_non_offending_methods
    source: |
      File.exist?("path/to/file")
    offenses:
      []
    ruby_version: ">= 2.3"
