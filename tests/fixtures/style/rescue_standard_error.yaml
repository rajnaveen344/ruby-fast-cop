cop: Style/RescueStandardError
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_no_error_class
    source: |
      begin
        foo
      rescue
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_no_error_class_assigned_to_a_variable
    source: |
      begin
        foo
      rescue => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_a_single_error_class_other_than_standarderror
    source: |
      begin
        foo
      rescue BarError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_a_single_error_class_other_than_standarderror_assigned_to_a_variable
    source: |
      begin
        foo
      rescue BarError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_when_rescuing_standarderror_by_itself_registers_an_offense
    source: |
      begin
        foo
      rescue StandardError
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 20
        message: Omit the error class when rescuing `StandardError` by itself.
    corrected: |
      begin
        foo
      rescue
        bar
      end
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_when_rescuing_standarderror_by_itself_with_standarderror_registers_an_offense
    source: |
      begin
        foo
      rescue ::StandardError
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 22
        message: Omit the error class when rescuing `StandardError` by itself.
    corrected: |
      begin
        foo
      rescue
        bar
      end
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_when_rescuing_standarderror_by_itself_when_the_error_is_assigned_to_a_variable_registers_an_offense
    source: |
      begin
        foo
      rescue StandardError => e
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 20
        message: Omit the error class when rescuing `StandardError` by itself.
    corrected: |
      begin
        foo
      rescue => e
        bar
      end
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_when_rescuing_standarderror_by_itself_when_the_error_is_assigned_to_a_variable_with_standarderror_registers_an_offense
    source: |
      begin
        foo
      rescue ::StandardError => e
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 22
        message: Omit the error class when rescuing `StandardError` by itself.
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_with_other_errors
    source: |
      begin
        foo
      rescue StandardError, BarError
        bar
      rescue BazError, StandardError
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_with_other_errors
    source: |
      begin
        foo
      rescue ::StandardError, BarError
        bar
      rescue ::BazError, ::StandardError
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_with_other_errors_assigned_to_a_variable
    source: |
      begin
        foo
      rescue StandardError, BarError => e
        bar
      rescue BazError, StandardError => e
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_no_error_class
    source: |
      def baz
        foo
      rescue
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_no_error_class_assigned_to_a_variable
    source: |
      def baz
        foo
      rescue => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_a_single_error_other_than_standarderror
    source: |
      def baz
        foo
      rescue BarError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_a_single_error_other_than_standarderror_assigned_to_a_variable
    source: |
      def baz
        foo
      rescue BarError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_when_rescuing_standarderror_by_itself_registers_an_offense
    source: |
      def foobar
        foo
      rescue StandardError
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 20
        message: Omit the error class when rescuing `StandardError` by itself.
    corrected: |
      def foobar
        foo
      rescue
        bar
      end
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_when_rescuing_standarderror_by_itself_when_the_error_is_assigned_to_a_variable_registers_an_offense
    source: |
      def foobar
        foo
      rescue StandardError => e
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 20
        message: Omit the error class when rescuing `StandardError` by itself.
    corrected: |
      def foobar
        foo
      rescue => e
        bar
      end
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_with_other_errors
    source: |
      def foobar
        foo
      rescue StandardError, BarError
        bar
      rescue BazError, StandardError
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_with_other_errors_assigned_to_a_variable
    source: |
      def foobar
        foo
      rescue StandardError, BarError => e
        bar
      rescue BazError, StandardError => e
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_implicit_accepts_rescue_modifier
    source: |
      foo rescue 42
    offenses:
      []
    config:
      EnforcedStyle: implicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_when_calling_rescue_without_an_error_class_registers_an_offense
    source: |
      begin
        foo
      rescue
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 6
        message: Avoid rescuing without specifying an error class.
    corrected: |
      begin
        foo
      rescue StandardError
        bar
      end
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_when_calling_rescue_without_an_error_class_when_the_error_is_assigned_to_a_variable_registers_an_offense
    source: |
      begin
        foo
      rescue => e
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 6
        message: Avoid rescuing without specifying an error class.
    corrected: |
      begin
        foo
      rescue StandardError => e
        bar
      end
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_a_single_error_other_than_standarderror
    source: |
      begin
        foo
      rescue BarError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_a_single_error_other_than_standarderror_assigned_to_a_variable
    source: |
      begin
        foo
      rescue BarError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_by_itself
    source: |
      begin
        foo
      rescue StandardError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_by_itself_assigned_to_a_variable
    source: |
      begin
        foo
      rescue StandardError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_with_other_errors
    source: |
      begin
        foo
      rescue StandardError, BarError
        bar
      rescue BazError, StandardError
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_begin_block_accepts_rescuing_standarderror_with_other_errors_assigned_to_a_variable
    source: |
      begin
        foo
      rescue StandardError, BarError => e
        bar
      rescue BazError, StandardError => e
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_when_rescue_is_called_without_an_error_class_registers_an_offense
    source: |
      def baz
        foo
      rescue
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 6
        message: Avoid rescuing without specifying an error class.
    corrected: |
      def baz
        foo
      rescue StandardError
        bar
      end
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_when_the_error_is_assigned_to_a_variable_registers_an_offense
    source: |
      def baz
        foo
      rescue => e
        bar
      end
    offenses:
      - line: 3
        column_start: 0
        column_end: 6
        message: Avoid rescuing without specifying an error class.
    corrected: |
      def baz
        foo
      rescue StandardError => e
        bar
      end
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_a_single_error_other_than_standarderror
    source: |
      def baz
        foo
      rescue BarError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_a_single_error_other_than_standarderror_assigned_to_a_variable
    source: |
      def baz
        foo
      rescue BarError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_by_itself
    source: |
      def foobar
        foo
      rescue StandardError
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_by_itself_assigned_to_a_variable
    source: |
      def foobar
        foo
      rescue StandardError => e
        bar
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_with_other_errors
    source: |
      def foobar
        foo
      rescue StandardError, BarError
        bar
      rescue BazError, StandardError
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_when_rescuing_in_a_method_definition_accepts_rescuing_standarderror_with_other_errors_assigned_to_a_variable
    source: |
      def foobar
        foo
      rescue StandardError, BarError => e
        bar
      rescue BazError, StandardError => e
        baz
      end
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]

  - name: rubocop_cop_style_rescuestandarderror_explicit_accepts_rescue_modifier
    source: |
      foo rescue 42
    offenses:
      []
    config:
      EnforcedStyle: explicit
      SupportedStyles: [implicit, explicit]
