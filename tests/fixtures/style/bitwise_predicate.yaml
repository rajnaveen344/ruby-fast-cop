cop: Style/BitwisePredicate
department: style
severity: convention
implemented: false

tests:
  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_predicate_for_comparisons
    source: |
      (variable & flags).positive?
    offenses:
      - line: 1
        column_start: 0
        column_end: 28
        message: Replace with `variable.anybits?(flags)` for comparison with bit flags.
    corrected: |
      variable.anybits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_0_for_comparisons
    source: |
      (variable & flags) > 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 22
        message: Replace with `variable.anybits?(flags)` for comparison with bit flags.
    corrected: |
      variable.anybits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_1_for_comparisons
    source: |
      (variable & flags) >= 1
    offenses:
      - line: 1
        column_start: 0
        column_end: 23
        message: Replace with `variable.anybits?(flags)` for comparison with bit flags.
    corrected: |
      variable.anybits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_0_for_comparisons
    source: |
      (variable & flags) != 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 23
        message: Replace with `variable.anybits?(flags)` for comparison with bit flags.
    corrected: |
      variable.anybits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_does_not_register_an_offense_when_using_anybits_method
    source: |
      variable.anybits?(flags)
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_does_not_register_an_offense_when_using_in_conjunction_with_1_for_comparisons
    source: |
      (variable & flags) > 1
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_5_does_not_register_an_offense_when_comparing_with_no_parentheses
    source: |
      foo == bar
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_any_set_bits_when_ruby_2_4_does_not_register_an_offense_when_using_in_conjunction_with_predicate_for_comparisons
    source: |
      (variable & flags).positive?
    offenses:
      []
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_all_set_bits_when_ruby_2_5_registers_an_offense_when_using_with_rhs_flags_in_conjunction_with_for_comparisons
    source: |
      (variable & flags) == flags
    offenses:
      - line: 1
        column_start: 0
        column_end: 27
        message: Replace with `variable.allbits?(flags)` for comparison with bit flags.
    corrected: |
      variable.allbits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_all_set_bits_when_ruby_2_5_registers_an_offense_when_using_with_lhs_flags_in_conjunction_with_for_comparisons
    source: |
      (flags & variable) == flags
    offenses:
      - line: 1
        column_start: 0
        column_end: 27
        message: Replace with `variable.allbits?(flags)` for comparison with bit flags.
    corrected: |
      variable.allbits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_all_set_bits_when_ruby_2_5_does_not_register_an_offense_when_using_allbits_method
    source: |
      variable.allbits?(flags)
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_all_set_bits_when_ruby_2_5_does_not_register_an_offense_when_flag_variable_names_are_mismatched
    source: |
      (flags & variable) == flagments
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_all_set_bits_when_ruby_2_4_does_not_register_an_offense_when_using_with_rhs_flags_in_conjunction_with_for_comparisons
    source: |
      (variable & flags) == flags
    offenses:
      []
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_no_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_zero_for_comparisons
    source: |
      (variable & flags).zero?
    offenses:
      - line: 1
        column_start: 0
        column_end: 24
        message: Replace with `variable.nobits?(flags)` for comparison with bit flags.
    corrected: |
      variable.nobits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_no_set_bits_when_ruby_2_5_registers_an_offense_when_using_in_conjunction_with_0_for_comparisons
    source: |
      (variable & flags) == 0
    offenses:
      - line: 1
        column_start: 0
        column_end: 23
        message: Replace with `variable.nobits?(flags)` for comparison with bit flags.
    corrected: |
      variable.nobits?(flags)
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_no_set_bits_when_ruby_2_5_does_not_register_an_offense_when_using_nobits_method
    source: |
      variable.nobits?(flags)
    offenses:
      []
    ruby_version: ">= 2.5"

  - name: rubocop_cop_style_bitwisepredicate_when_checking_no_set_bits_when_ruby_2_4_does_not_register_an_offense_when_using_in_conjunction_with_zero_for_comparisons
    source: |
      (variable & flags).zero?
    offenses:
      []
    ruby_version: ">= 2.4"

  - name: rubocop_cop_style_bitwisepredicate_when_using_a_simple_method_call_does_not_register_an_offense
    source: |
      zero?
    offenses:
      []
